{
  "name": "vtc-rag-backend",
  "version": "1.0.0",
  "description": "Backend API cho hệ thống trợ lý học tập RAG",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node scripts/check-syntax.js",
    "test:contract": "node scripts/rag-contract-test.js",
    "test:part2": "node scripts/part2-smoke.js",
    "docker:mock:config": "docker compose config --quiet",
    "docker:mock:up": "docker compose up -d --build",
    "docker:mock:ps": "docker compose ps",
    "docker:mock:logs:app": "docker compose logs app --tail 200",
    "docker:mock:down": "docker compose down",
    "docker:mock:reset": "docker compose down -v --remove-orphans",
    "docker:remote:config": "docker compose --profile rag -f docker-compose.yml -f docker-compose.remote.yml config --quiet",
    "docker:remote:up": "docker compose --profile rag -f docker-compose.yml -f docker-compose.remote.yml up -d --build",
    "docker:remote:ps": "docker compose --profile rag -f docker-compose.yml -f docker-compose.remote.yml ps",
    "docker:remote:logs:app": "docker compose --profile rag -f docker-compose.yml -f docker-compose.remote.yml logs app --tail 200",
    "docker:remote:logs:rag": "docker compose --profile rag -f docker-compose.yml -f docker-compose.remote.yml logs rag-service --tail 200",
    "docker:remote:files:node": "docker compose --profile rag -f docker-compose.yml -f docker-compose.remote.yml exec -T app find /usr/src/app/uploads/documents -type f",
    "docker:remote:files:rag": "docker compose --profile rag -f docker-compose.yml -f docker-compose.remote.yml exec -T rag-service find /shared/uploads/documents -type f",
    "docker:remote:mysql": "docker compose --profile rag -f docker-compose.yml -f docker-compose.remote.yml exec db mysql -uroot -p edurag",
    "docker:remote:down": "docker compose --profile rag -f docker-compose.yml -f docker-compose.remote.yml down",
    "docker:remote:reset": "docker compose --profile rag -f docker-compose.yml -f docker-compose.remote.yml down -v --remove-orphans",
    "preflight:remote": "node scripts/remote-preflight.js",
    "test:remote": "node scripts/remote-e2e-smoke.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.2.0",
    "mysql2": "^3.9.7",
    "swagger-jsdoc": "^6.3.0",
    "swagger-ui-express": "^5.0.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
