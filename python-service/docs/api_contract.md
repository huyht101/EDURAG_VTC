# API contract pointer

The canonical NodeJS–Python contract is:

[`../../docs/api/internal-rag-contract.md`](../../docs/api/internal-rag-contract.md)

Python runtime schemas remain in [`../models/schemas.py`](../models/schemas.py). Do not duplicate request/response examples here; update the root contract and shared fixtures together.
