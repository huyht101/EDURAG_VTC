# Issue and quality register

Updated: 2026-08-02. Only evidence-backed items are listed. Taxonomy is singular per
row: `DEFECT`, `CONTRACT_GAP`, `VERIFICATION_GAP`, `OVERCOMPLEXITY`, `REDUNDANCY`,
`MAINTAINABILITY`, or `PERFORMANCE`. P0/P1/P2 expresses delivery order, not severity.

| ID / issue | Category | Evidence | Severity / impact | Requirement or risk | Documentation action | Required fix, decision or simplification | Invariants | Priority | Owner / next action |
|---|---|---|---|---|---|---|---|---|---|
| `PY-MD-001` — upstream delivery of repaired citation markers | VERIFICATION_GAP | Worktree `rag_engine.py::_extract_citations()` now excludes inline/fenced code and array indexing, keeps valid mixed markers and resolves only citable results; Python offline suite passes. | MEDIUM delivery risk: snapshot fix is not yet an accepted Python-upstream revision or live generation trace. | Citation correctness in prose/table while preserving code, LaTeX and structured sources. | Canonical Python handoff records the repaired behavior and evidence boundary. | Review and upstream the current patch; run live generation only in the approved provider phase. | `answer` remains a string; structured citations authoritative; Node does not parse markers. | **P0 delivery** | Python team / Owner review |
| `RAG-REC-001` — durable pre-ACK execution remains | MAINTAINABILITY | Worktree has bounded post-ACK activation retry, residual logs, consistency inspection and exact manual recovery; offline Qdrant E2E passes. FastAPI `BackgroundTasks` still does not resume work lost before callback/ACK. | MEDIUM post-MVP reliability limitation; MVP has fail-closed operator recovery but no automatic durable execution. | Whole-document outcome and exact-attempt recovery without duplicate cost/data. | Project/Python handoffs distinguish verified MVP recovery from durable execution later. | Keep explicit operator verification for current READY attempt; consider durable queue/scheduler only after MVP operational evidence. | Whole-document success/failure; exact-attempt idempotency; no active orphan; Node/Python persistence ownership. | **P2 POST-MVP** | Node + Python operations |
| `INT-LIVE-001` — live provider acceptance | VERIFICATION_GAP | `test:rag-production-offline-e2e` passes with the production Node app, disposable MySQL, current Python HTTP service and Qdrant, but OCR/embedding/LLM are deterministic local providers. | MEDIUM Phase 2 confidence gap: provider quality, quota, privacy and real model behavior are not proven. | Live acceptance must use the exact reviewed revisions without weakening offline lifecycle invariants. | Project handoff records production offline evidence separately from live-provider evidence. | Run the approved Phase 2 isolated provider/corpus scenarios and record revisions, models and checksums without secrets. | No canonical mutation; exact-attempt lifecycle; disposable namespace; secrets redacted. | **P1 PHASE 2** | Integration owner with Node + Python teams |
| `CORPUS-EQ-001` — selected-release identity/equivalence | VERIFICATION_GAP — CLOSED | Pointer now selects `v1-d07f526e059e53751402a4f3`; create-only publish, remote read-back, clean restore, 81-chunk/point lifecycle consistency, originals and citation/RBAC acceptance passed before pointer update and local reset. | Previous HIGH gap is closed for the selected release; future pointer changes reopen the verification requirement. | A selected private release must have verified manifest/checksums and equivalent MySQL, Qdrant and original-file state. | Project handoff, corpus architecture and MVP matrix record the accepted release and evidence boundary. | No current fix. Repeat the same pointer-last acceptance for future releases. | Immutable releases; no overwrite; checksum/inventory guards; Node/Python store ownership. | **CLOSED** | Corpus operator / DevOps; preserve evidence with the release change |
| `PY-OCR-001` — live OCR acceptance | VERIFICATION_GAP | Worktree validates explicit `OFF|AUTO`, never enables OCR from key presence alone and has deterministic digital/scanned/mixed/blank/failure tests. No approved live provider run was made. | MEDIUM acceptance gap: provider quality/privacy/quota/timeout behavior is unproven live; offline contract is stable. | OCR-enabled MVP must fail whole-document when required OCR is unusable. | Python/project handoffs separate offline implementation from live acceptance. | Upstream the patch, then run approved live Vietnamese/rotation/provider acceptance without silent fallback. | Whole-document outcome; canonical PDF/page provenance; no Node API/schema change. | **P1 live phase** | Python + Owner/provider operator |
| `PY-EVAL-001` — evaluator repair delivery | VERIFICATION_GAP | Worktree evaluator refuses the canonical collection, requires an explicit disposable target and has behavioral guard tests; default tests run network-off. | LOW until evaluator is used; upstream revision is not yet recorded. | Evaluation must be isolated, reproducible and fail closed. | Python handoff records disposable-only behavior. | Review/upstream the patch and retain non-zero safety assertion behavior. | Python owns Qdrant; no canonical/provider mutation in offline tests; deterministic evidence. | **P1 delivery** | Python team |
| `DB-MIG-001` — partial DDL without ledger row | DEFECT | `scripts/migrate.js` executes each MySQL DDL statement before inserting `schema_migrations`; MySQL DDL auto-commits. | HIGH for a future interrupted multi-statement migration; blind rerun can collide with partial schema. | Existing-data migration requires recoverable, exact-target execution. | Database README records the limitation and forbids assuming an absent ledger row means no DDL ran. | Add per-migration pre/postconditions or an explicit repair runbook before the next migration/deployment that needs DDL. | Backup, data preservation, no false ledger entry, rollback/repair safety. | **P1 before next migration** | Node/Core + DevOps |
| `PY-LOC-001` — no Python geometry path | CONTRACT_GAP | Node/schema validate and persist `sourceLocator`; repository search finds no Python parser/chunk/Qdrant production path generating it. | MEDIUM: precise highlight unavailable; baseline citation remains usable via document name, `pageNumber` and `sourceText`. | Locator correctness matters only when precise highlight is selected; it is not a baseline MVP blocker. | Python/source-locator handoffs state nullable fallback and OPTIONAL/LATER highlight explicitly. | If prioritized, generate occurrence-specific page-bounded boxes, preserve through Qdrant/query and verify visually; otherwise keep null. | Immutable snapshots; normalized boxes; no fake/full-page boxes; fallback remains valid. | **P2 / before precise-highlight UI only** | Python + FE teams if Owner prioritizes |
| `DOC-ART-001` — duplicate artifact resolver rules | REDUNDANCY | `document-dto-service.js::canonicalDownloadArtifact()` and `document-ingest-service.js::ingestArtifact()` separately encode PDF/DOCX/TXT mapping. | MEDIUM future drift risk; current contract tests cover the mapping. | Preview/download and ingest must reference the same canonical artifact. | Project handoff states one artifact matrix; issue remains implementation-only. | Consider one internal resolver with explicit purpose modes after MVP; retain byte/mapping regressions. | RBAC/IDOR; no storage-key exposure; no regeneration; current public contract. | **P2 POST-MVP** | Node/Core |
| `TEST-HARNESS-001` — monolithic Part 2 smoke | MAINTAINABILITY | `scripts/part2-smoke.js` spans auth, assets, documents, Library, callbacks, chat and cleanup in one scenario. | MEDIUM maintenance cost; no demonstrated contract failure. | Failures should be localizable without weakening full acceptance. | Testing evidence is classified by boundary; no claim relies solely on this harness. | Split reusable domain fixtures/helpers while retaining one isolated top-level acceptance command. | Disposable namespace, cleanup, real HTTP/MySQL and deterministic no-provider behavior. | **P2 POST-MVP** | Node QA |
| `FE-CD-001` — filename header not exposed to browser JS | CONTRACT_GAP | Responses set `Content-Disposition`, but current CORS configuration does not expose it to cross-origin JavaScript. | LOW: authenticated download works; FE may need metadata fallback for filename. | FE must not infer storage paths or lose authenticated download behavior. | FE guide documents metadata fallback and does not promise readable header. | Add the narrow exposed header in a later compatible patch only if FE requires it. | Bearer auth; RFC 5987 safety; no public storage URL. | **P2 unless FE blocks** | FE confirms need; Node implements if required |
| `CORPUS-SIG-001` — signal-time staging cleanup | MAINTAINABILITY | Corpus publisher signal handler resumes writers best-effort, while direct process exit does not guarantee normal/error `finally` staging cleanup. | LOW: abandoned local staging may need manual cleanup; pointer-last protects current release. | Operator interruption must not corrupt selected release or leave writers paused. | Corpus architecture records the best-effort limitation. | Route signals through normal cleanup when tooling is next changed. | Never delete immutable remote release; verify-before-pointer; writer resume. | **P2 POST-MVP** | Node corpus tooling |
| `TOOL-DEP-001` — transitive `punycode` warning | MAINTAINABILITY | `corpus:inspect` emits `DEP0040`; tree is `@google-cloud/storage@7.21.0 → gaxios@6.7.1 → node-fetch@2.7.0 → whatwg-url@5.0.0 → tr46@0.0.3`. | LOW non-blocking compatibility debt; inspect succeeds. | Tooling should remain compatible without suppressing runtime warnings. | Register records exact tree; no readiness claim treats warning as failure. | Upgrade through a compatible direct dependency release after corpus regression; do not suppress. | Private-target/checksum/create-only guards; lockfile review. | **P2 POST-MVP** | Node corpus tooling |

## Documentation issues closed

- Week 3/4 readiness files are explicitly historical and no longer current status.
- Random Week 5 simulation is not retrieval-quality evidence or configuration advice.
- Node locator support is separated from optional Python geometry/FE highlight.
- Python action details have one canonical handoff; the OCR/Markdown file is a
  supporting historical finding record only.
