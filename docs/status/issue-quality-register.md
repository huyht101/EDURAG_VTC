# Issue and quality register

Updated: 2026-08-11. Only evidence-backed items are listed. Taxonomy is singular per
row: `DEFECT`, `CONTRACT_GAP`, `VERIFICATION_GAP`, `OVERCOMPLEXITY`, `REDUNDANCY`,
`MAINTAINABILITY`, or `PERFORMANCE`. P0/P1/P2 expresses delivery order, not severity.
Live-provider and corpus results are recorded isolated evidence at commit `23afbec`, not
a current rerun or proof that current external state and operational gaps are resolved.
The separate 2026-08-11 LlamaParse probe is current fixture-bounded evidence, not a full
cross-runtime acceptance or provider guarantee. Other PASS references are
repository-recorded evidence unless explicitly labeled as a current rerun.

| ID / issue | Category | Evidence | Severity / impact | Requirement or risk | Documentation action | Required fix, decision or simplification | Invariants | Priority | Owner / next action |
|---|---|---|---|---|---|---|---|---|---|
| `PY-MD-001` — upstream delivery of repaired citation markers | VERIFICATION_GAP | Snapshot `rag_engine.py::_extract_citations()` excludes inline/fenced code and array indexing, keeps valid mixed markers and resolves only citable results; an offline-suite PASS is recorded but was not rerun for this consolidation. | MEDIUM delivery risk: snapshot fix is not yet an accepted Python-upstream revision or current live generation trace. | Citation correctness in prose/table while preserving code, LaTeX and structured sources. | Canonical Python handoff records the repaired behavior and evidence boundary. | Review and upstream only under separate scope; run live generation only in an approved provider phase. | `answer` remains a string; structured citations authoritative; Node does not parse markers. | **P0 delivery / DEFERRED; UPSTREAM UNKNOWN** | Python team / Owner review |
| `RAG-REC-001` — bounded recovery/reconciliation and ownership | MAINTAINABILITY | Snapshot has bounded post-ACK activation retry, residual logs, consistency inspection and exact manual recovery; recorded offline evidence exists. `BackgroundTasks` does not resume pre-ACK lost work and the CLI requires operator confirmation outside MySQL. | MEDIUM operational-acceptance gap; implementation presence does not decide the required gate or owner. | Whole-document outcome and exact-attempt recovery without duplicate cost/data. | Root/Python handoffs keep offline implementation separate from operational acceptance. | Preserve the bounded behavior; Owner/Node/Python/operator must decide ownership and acceptance. Do not infer a durable queue requirement. | Whole-document success/failure; exact-attempt idempotency; no active orphan; persistence ownership. | **UNRESOLVED BEFORE OPERATIONAL ACCEPTANCE** | **OWNER/CROSS-TEAM DECISION REQUIRED** |
| `INT-LIVE-001` — current live provider acceptance | VERIFICATION_GAP | Commit `23afbec` records isolated digital/scanned provider E2E on the revisions used by that run; production offline evidence also exists. It was not rerun for baseline `f269334`. The 2026-08-11 page probe called only the parser with a synthetic fixture and is not Node→Python→Qdrant E2E. | MEDIUM confidence gap for current provider/runtime state; neither evidence set closes recovery or visibility ordering. | Live evidence must remain pinned to exact revisions and disposable scope. | Project handoff labels each evidence boundary. | Require explicit Owner scope for any future integration rerun. | No canonical mutation; exact-attempt lifecycle; disposable namespace; secrets redacted. | **PREVIOUS_REPORT_ONLY AT `23afbec` / CURRENT CROSS-RUNTIME STATE UNVERIFIED** | Integration owner with Node + Python teams |
| `CORPUS-EQ-001` — historical release-ID equivalence | VERIFICATION_GAP | Historical evidence mentions `v1-7463f169257976a90e65ab7c`, but repository metadata does not establish whether it is a predecessor, a logically equivalent export or a different canonical state relative to the selected release. | MEDIUM provenance gap: historical release identity/equivalence must not be inferred without comparing the relevant manifests/fingerprints. | Preserve the relationship question separately from acceptance of any one pinned release. | Corpus authority keeps the historical-ID boundary explicit. | Keep unresolved until separately authorized read-only evidence establishes the relationship; do not infer completion from pointer metadata or `23afbec`. | Immutable releases; no overwrite; exact revision/release scope; no inferred cross-store equivalence. | **DEFERRED / UNRESOLVED** | Corpus operator / Owner review |
| `CORPUS-ACC-001` — selected-release recorded acceptance | VERIFICATION_GAP | Commit `23afbec` records isolated create-only publish, remote read-back, clean restore, lifecycle consistency, originals and citation/RBAC acceptance for `v1-d07f526e059e53751402a4f3`. This was not rerun for the current checkpoint. | The pinned run is recorded evidence only; it does not prove current remote state or close exact-key and GCS/archive-key reviews. | A selected private release must have verified manifest/checksums and equivalent stores for the exact acceptance run. | Project handoff and MVP matrix preserve the pinned evidence boundary. | Repeat pointer-last acceptance for future release changes; keep current remote-state, exact-key and GCS/archive-key reviews unresolved until separately evidenced. | Immutable releases; no overwrite; checksum/inventory guards; store ownership. | **RECORDED ISOLATED EVIDENCE AT `23afbec` / NOT CURRENT RERUN** | Corpus operator / DevOps |
| `PY-OCR-001` — live OCR acceptance | VERIFICATION_GAP | Snapshot validates explicit `OFF|AUTO` with deterministic fixtures. Commit `23afbec` records isolated scanned-provider E2E (**PREVIOUS_REPORT_ONLY**). The current two-run page fixture observed the blank sentinel and image-only OCR consistently, but did not exercise broader failure/privacy/quota behavior. | MEDIUM current-state and provenance gap: bounded OCR observation is not full acceptance, and sparse page identity is not established. | Required OCR must fail whole-document when unusable and must not claim an untrusted page. | Python/project handoffs separate offline behavior, recorded integration and bounded page evidence. | Future upstream/live acceptance needs separate scope; do not infer general provider behavior from the fixture. | Whole-document outcome; canonical PDF/page provenance; no Node API/schema change. | **CURRENT_VERIFIED BOUNDED PROBE / GENERAL ACCEPTANCE UNVERIFIED** | Python + Owner/provider operator |
| `PY-PAGE-001` — LlamaParse physical-page alignment | CONTRACT_GAP | Repository pins `llama-parse==0.6.4`; adapter uses `split_by_page=True` and enumerates output position. Two identical live jobs returned all four fixture items (blank sentinel and image-only OCR included) with empty metadata and no explicit page field. The mixed-PDF unit test still mocks below the adapter boundary. | HIGH residual citation-provenance risk when provider output is actually omitted, merged or sparse; the fixture did not reproduce that condition. | Any claimed page must identify the canonical PDF physical page; geometry may remain null. | Root, Python and locator handoffs retain exact fixture/probe scope. | No repair is justified on the legacy result. Preserve the limitation for reporting; any future provider/client migration requires separate scope/evidence. | 1-based canonical physical page; no fabricated mapping or geometry. | **FIXTURE DID NOT REPRODUCE — RESIDUAL RISK DOCUMENTED** | Report as limitation; future Owner/Python decision only if work resumes |
| `RAG-VIS-001` — stale hide/unhide ordering | CONTRACT_GAP | Node blocks concurrent active operations and rejects stale callbacks; Python hide/unhide mutates all Qdrant points matching `doc_id` before callback outcome. | HIGH operational ordering risk: a late operation may mutate retrieval state despite a stale callback rejection. | Stale/conflicting operations must not reverse newer visibility state. | Root/Python handoffs record the intended-versus-implemented gap. | No wire/versioning change, recovery owner or ordering policy is selected in this documentation task. | Node business authority; Python Qdrant ownership; stale safety. | **DEFERRED / UNRESOLVED** | Owner + Node + Python decision |
| `PY-EVAL-001` — evaluator repair delivery | VERIFICATION_GAP | Snapshot evaluator refuses the canonical collection, requires an explicit disposable target and has behavioral guard tests; recorded default tests run network-off, not rerun here. | LOW until evaluator is used; upstream revision is not yet recorded. | Evaluation must be isolated, reproducible and fail closed. | Python handoff records disposable-only behavior. | Review/upstream under separate scope and retain non-zero safety assertion behavior. | Python owns Qdrant; no canonical/provider mutation in offline tests; deterministic evidence. | **P1 delivery / DEFERRED; UPSTREAM UNKNOWN** | Python team |
| `DB-MIG-001` — partial DDL without ledger row | DEFECT | `scripts/migrate.js` executes each MySQL DDL statement before inserting `schema_migrations`; MySQL DDL auto-commits. | HIGH for a future interrupted multi-statement migration; blind rerun can collide with partial schema. | Existing-data migration requires recoverable, exact-target execution. | Database README records the limitation and forbids assuming an absent ledger row means no DDL ran. | Add per-migration pre/postconditions or an explicit repair runbook before the next migration/deployment that needs DDL. | Backup, data preservation, no false ledger entry, rollback/repair safety. | **P1 before next migration** | Node/Core + DevOps |
| `PY-LOC-001` — no Python geometry path | CONTRACT_GAP | Node/schema validate and persist `sourceLocator`; no Python production path generates it. | MEDIUM for precise highlight; nullable geometry does not resolve `PY-PAGE-001`. | Geometry is optional, but any page claim remains a baseline provenance requirement. | Python/source-locator handoffs separate page identity from optional boxes. | If prioritized, generate occurrence-specific page-bounded boxes; otherwise keep null. Never fabricate full-page boxes. | Immutable snapshots; normalized boxes; trustworthy page first. | **P2 / before precise-highlight UI only** | Python + FE teams if Owner prioritizes |
| `DOC-ART-001` — duplicate artifact resolver rules | REDUNDANCY | `document-dto-service.js::canonicalDownloadArtifact()` and `document-ingest-service.js::ingestArtifact()` separately encode PDF/DOCX/TXT mapping. | MEDIUM future drift risk; current contract tests cover the mapping. | Preview/download and ingest must reference the same canonical artifact. | Project handoff states one artifact matrix; issue remains implementation-only. | Consider one internal resolver with explicit purpose modes after MVP; retain byte/mapping regressions. | RBAC/IDOR; no storage-key exposure; no regeneration; current public contract. | **P2 POST-MVP** | Node/Core |
| `TEST-HARNESS-001` — monolithic Part 2 smoke | MAINTAINABILITY | `scripts/part2-smoke.js` spans auth, assets, documents, Library, callbacks, chat and cleanup in one scenario. | MEDIUM maintenance cost; no demonstrated contract failure. | Failures should be localizable without weakening full acceptance. | Testing evidence is classified by boundary; no claim relies solely on this harness. | Split reusable domain fixtures/helpers while retaining one isolated top-level acceptance command. | Disposable namespace, cleanup, real HTTP/MySQL and deterministic no-provider behavior. | **P2 POST-MVP** | Node QA |
| `FE-CD-001` — filename header not exposed to browser JS | CONTRACT_GAP | Responses set `Content-Disposition`, but current CORS configuration does not expose it to cross-origin JavaScript. | LOW: authenticated download works; FE may need metadata fallback for filename. | FE must not infer storage paths or lose authenticated download behavior. | FE guide documents metadata fallback and does not promise readable header. | Add the narrow exposed header in a later compatible patch only if FE requires it. | Bearer auth; RFC 5987 safety; no public storage URL. | **P2 unless FE blocks** | FE confirms need; Node implements if required |
| `CORPUS-SIG-001` — signal-time staging cleanup | MAINTAINABILITY | Corpus publisher signal handler resumes writers best-effort, while direct process exit does not guarantee normal/error `finally` staging cleanup. | LOW: abandoned local staging may need manual cleanup; pointer-last protects current release. | Operator interruption must not corrupt selected release or leave writers paused. | Corpus architecture records the best-effort limitation. | Route signals through normal cleanup when tooling is next changed. | Never delete immutable remote release; verify-before-pointer; writer resume. | **P2 POST-MVP** | Node corpus tooling |
| `CORPUS-QDRANT-001` — historical bootstrap request failure provenance | VERIFICATION_GAP | `f269334` makes `CORPUS_QDRANT_REQUEST_FAILED` preserve phase, method, sanitized target and transport cause; non-success HTTP errors preserve status and redacted first-line body. Local loopback tests cover unavailable and 401 responses. Original member command/log/environment are absent, so the member incident was not reproduced. | MEDIUM diagnostic/provenance gap for that incident; current failures are actionable without proving the historical cause. | Fail fast for configuration/auth/integrity errors; no blind retry; never expose secrets. | Corpus authority distinguishes the diagnostic correction from incident closure. | Collect exact command, exit code, redacted env targets and Docker status/log if the member issue recurs. Do not call it fixed without equivalent cause. | Exact target/key/integrity guards; sanitized errors; no remote mutation. | **DIAGNOSTICS CORRECTED / INCIDENT PLAUSIBLE-UNVERIFIED** | Corpus operator / affected member |
| `TOOL-DEP-001` — transitive `punycode` warning | MAINTAINABILITY | `corpus:inspect` emits `DEP0040`; tree is `@google-cloud/storage@7.21.0 → gaxios@6.7.1 → node-fetch@2.7.0 → whatwg-url@5.0.0 → tr46@0.0.3`. | LOW non-blocking compatibility debt; inspect succeeds. | Tooling should remain compatible without suppressing runtime warnings. | Register records exact tree; no readiness claim treats warning as failure. | Upgrade through a compatible direct dependency release after corpus regression; do not suppress. | Private-target/checksum/create-only guards; lockfile review. | **P2 POST-MVP** | Node corpus tooling |

## Documentation issues closed

- Week 3/4 readiness/test-plan files are explicitly
  `HISTORICAL — NOT CURRENT AUTHORITY` and no longer current status.
- Random Week 5 simulation is not retrieval-quality evidence or configuration advice;
  the unreferenced companion CSV was removed during final data cleanup.
- Node locator support is separated from optional Python geometry/FE highlight.
- Python action details have one canonical handoff; the OCR/Markdown file is a
  supporting historical finding record only.
